'use client';

// DetailsStep is now deprecated — its functionality was split into:
// PersonalDataStep (step 1) and ImageUploadStep (step 4) in the new BookingWizard flow.
// This stub prevents import errors from any remaining references.

export default function DetailsStep() {
    return null;
}
